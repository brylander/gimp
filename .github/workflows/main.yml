name: GIMP tests & SonarCloud

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build-and-analyze:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            meson \
            ninja-build \
            pkg-config \
            gettext \
            libgtk-3-dev \
            libjpeg-dev \
            libpng-dev \
            libtiff-dev \
            liblcms2-dev \
            libwebp-dev \
            libgexiv2-dev \
            libjson-glib-dev \
            libgirepository1.0-dev \
            libxpm-dev \
            libgdk-pixbuf2.0-dev \
            libcurl4-gnutls-dev \
            libmypaint-dev \
            python3 \
            python3-pip \
            lcov \
            gcovr \
            git

      - name: Build and install babl (new enough for GIMP)
        run: |
          git clone https://gitlab.gnome.org/GNOME/babl.git
          cd babl
          meson setup _build --prefix=/usr
          ninja -C _build
          sudo ninja -C _build install

      - name: Build and install gegl (depends on babl)
        run: |
          git clone https://gitlab.gnome.org/GNOME/gegl.git
          cd gegl
          meson setup _build --prefix=/usr
          ninja -C _build
          sudo ninja -C _build install

      - name: Configure GIMP (Meson) with coverage enabled
        run: |
          meson setup build \
            --buildtype=debug \
            -Db_coverage=true

      - name: Build GIMP
        run: |
          meson compile -C build

      - name: Run tests
        run: |
          meson test -C build --print-errorlogs

      - name: Generate coverage report (gcovr, SonarQube format)
        run: |
          # Look for gcov data in build/ and map back to sources
          gcovr \
            --sonarqube \
            --root . \
            --xml-pretty \
            -o coverage.xml
          ls -l coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
